import {request} from 'request-promise-native';

export class ApiClient{
    getTasks(){
        return new Promise((resolve,reject)=>{
            resolve(data);
            reject(err);

        })
    }
}